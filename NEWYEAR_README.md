# 🎄 Инструкция по управлению новогодним оформлением

## Что добавлено на сайт:

✅ **Падающие снежинки** - красивая анимация по всему сайту
✅ **Ёлочка в логотипе** - пульсирующая новогодняя иконка
✅ **Новогоднее приветствие** - всплывающее окно при первой загрузке страницы
✅ **Подарочная анимация на кнопках** - проплывающий подарок
✅ **Снежное свечение при наведении** - на карточках услуг
✅ **Обновлённые промо-баннеры** - с новогодней тематикой

---

## 🔴 КАК ОТКЛЮЧИТЬ после праздников

### Вариант 1: Полное отключение (рекомендуется)

Откройте файл `index.html` и найдите эти 2 строки:

```html
<!-- 🎄 НОВОГОДНЕЕ ОФОРМЛЕНИЕ - для отключения закомментируйте следующую строку -->
<link href="styles/newyear.css" rel="stylesheet">
```

и

```html
<!-- 🎄 НОВОГОДНИЙ СКРИПТ - для отключения закомментируйте следующую строку -->
<script src="js/newyear.js"></script>
```

Закомментируйте их так:

```html
<!-- 🎄 НОВОГОДНЕЕ ОФОРМЛЕНИЕ - для отключения закомментируйте следующую строку 
<link href="styles/newyear.css" rel="stylesheet">
-->
```

и

```html
<!-- 🎄 НОВОГОДНИЙ СКРИПТ - для отключения закомментируйте следующую строку 
<script src="js/newyear.js"></script>
-->
```

**Готово!** Новогоднее оформление отключено.

---

### Вариант 2: Удаление файлов

Если хотите полностью удалить новогоднее оформление:

1. Удалите файл `js/newyear.js`
2. Удалите файл `styles/newyear.css`
3. Удалите строки подключения из `index.html` (см. Вариант 1)

---

## 🟢 КАК ВКЛЮЧИТЬ обратно

Просто раскомментируйте строки, которые закомментировали выше:

```html
<link href="styles/newyear.css" rel="stylesheet">
<script src="js/newyear.js"></script>
```

---

## 📝 Дополнительные настройки

### Изменить количество снежинок:

Откройте `js/newyear.js` и найдите строку:
```javascript
const snowflakesCount = 50; // Количество снежинок
```

Измените число 50 на желаемое (рекомендуется 30-70).

### Отключить только приветствие:

В файле `js/newyear.js` закомментируйте строку:
```javascript
setTimeout(showNewYearGreeting, 2000);
```

Так:
```javascript
// setTimeout(showNewYearGreeting, 2000);
```

### Вернуть старую иконку в промо-баннере:

В `index.html` измените:
```html
<span class="promo-icon">🎄</span>
```
Обратно на:
```html
<span class="promo-icon">🔓</span>
```

---

## ⚡ Быстрый FAQ

**Q: Снежинки тормозят сайт на телефоне?**
A: Нет, я оптимизировал код. Но если заметите проблемы - уменьшите количество снежинок до 30.

**Q: Можно ли сделать снежинки разноцветными?**
A: Да! В файле `js/newyear.js` найдите `color: #fff` и измените на любой цвет.

**Q: Когда лучше отключить?**
A: После 14 января можно смело отключать.

---

## 📞 Нужна помощь?

Если что-то не получается - просто напишите мне, помогу! 😊

**Николай - IT-помощь в Поварово**
📱 +7 (906) 005-03-02